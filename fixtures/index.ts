import cart from './cart';
import food from './food';
import order from './order';
import restaurants from './restaurants';

const fixtures = {
  restaurants, cart, food, order,
};

export default fixtures;
