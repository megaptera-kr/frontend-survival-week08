import styled from 'styled-components';

const ItemWrapStyle = styled.ul`
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap:2.5rem;
  padding-bottom: 4rem;
`;

export default ItemWrapStyle;
